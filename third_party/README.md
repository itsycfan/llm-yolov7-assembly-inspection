# Third-Party Code

`yolov7/` is a working copy of the official YOLOv7 repository
(https://github.com/WongKinYiu/yolov7), used here for component/tool/step
detection. Its own git history was stripped when incorporating it here.

TODO (during GitHub upload step): convert this into a proper git submodule
pointing at the upstream repo (or your own fork, if you modified any files),
rather than committing it as plain vendored files.
