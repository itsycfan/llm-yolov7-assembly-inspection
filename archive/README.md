# Archive

`legacy_dev_history.zip` contains the pre-git manual version snapshots
(dated "Main + GUI" folders and iterative OPT.py versions) that predate
proper version control on this project. Kept for historical reference
only — not part of the active codebase.
